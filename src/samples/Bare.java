package org.example;

import org.one.One;
import org.two.Two;
import org.three.*;

public static class Bare extends org.one.One implements Two {
  public void doNothing(int i, Jane j){
    if (!(a==1)) r=r+4;
  }
}
